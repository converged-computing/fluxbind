# Test packed pattern (default).
# Requests 4 cores within a single socket. The result should be the
# first 4 available cores by os_index (e.g., 0, 1, 2, 3).
resources:
  - type: socket
    count: 1
    with:
      - type: core
        count: 4
        pattern: packed
