# Find the 8 cores that are physically closest to the first available GPU
resources:
  - type: machine
    count: 1
    with:
      - type: core
        count: 8
        affinity:
          type: gpu
        pattern: packed
